import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    list: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        padding: 10,
        paddingLeft: 0,
        paddingBottom: 0
    }
});